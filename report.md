

# 物理实验报告要求

## 适用范围与优先级

本文件规定近代物理实验报告的内容、数据处理、写作和排版要求。具体实验讲义、课程模板或指导教师有额外规定时，以具体规定为准。

本文件只规范实验报告，不替代预习报告要求。报告正式文档须使用 LaTeX 编写并编译生成 PDF；本文件仅作为要求与提交前检查清单，不作为最终报告文件。

## 报告整体结构

正式报告按以下顺序组织：

1. 实验题目和基本信息；
2. 摘要（中文及英文）；
3. 关键词（中文及英文）；
4. 一、引言；
5. 二、原理；
6. 三、实验；
7. 四、结果与分析讨论；
8. 五、结论和建议（总结）；
9. 六、参考文献；
10. 原始数据、程序或其他补充材料附录（有数据或教师要求时）。

2. 摘要；
3. 关键词；

### 实验题目与基本信息

报告抬头使用本次实验的完整实验题目。实验题目下方的基本信息固定排成两行：

- 第一行：作者（姓名）、学号；
- 第二行：指导老师、实验时间。

基本信息栏不列班级。字段留空时保留填写横线，不使用“待补充”文字占据填写位置。

### 摘要

摘要控制在 **100—200 字**，并能够独立概括全文，包含以下四个要素：

- 实验目的；
- 实验方法或技术路线；
- 主要结果；
- 主要结论。

在存在真实测量或计算结果时，摘要至少给出一个关键数值，例如测量值、拟合参数或相对误差。数据尚未提供时写“待补充”，不得为了满足数值要求编造结果。摘要不展开详细推导，避免只写背景或空泛评价。

正式报告应同时提供中文摘要和对应的英文摘要（Abstract）。英文摘要应忠实概括中文摘要的实验目的、方法、主要结果和结论，物理量、单位和关键数值保持一致，不额外添加中文摘要没有依据的结果。中文关键词后列出对应的英文关键词。

正式报告须同时给出**英文摘要**，其内容与中文摘要一致：实验目的、方法、主要结果与结论都要覆盖，物理量、单位与关键数值保持一致，不得添加中文摘要中没有依据的结果；中文关键词后列出对应的英文关键词。

### 关键词

摘要后列出 **3—5 个关键词**，建议使用分号分隔。关键词应覆盖实验对象、主要方法、研究现象或核心物理量，避免直接重复整句标题。

## 正文内容

### 一、引言

引言建议控制在约 **200—400 字**，用简短、连贯的语言介绍：

- 与本实验直接相关的背景，包括必要的发展历程、应用前景或实际用途；
- 本实验所研究问题的意义；
- 实验目的和需要解决的主要问题。

背景材料应服务于本次实验，不堆砌与实验无关的历史资料。

### 二、原理

原理部分建议控制在约 **300—800 字**。应在理解基础上，用简明语言叙述必要的物理规律、理论模型、关键公式及其适用条件。

- 不得照抄实验讲义，应进行归纳和重述；
- 公式中的符号在第一次出现时说明物理意义和单位；
- 必要时说明理论量与可观测量、测量量之间的关系；
- 说明公式成立所需的近似、边界条件或实验条件。

### 三、实验

介绍本次实验的实施过程，包括：

1. **实验仪器和装置**：主要仪器名称、用途、连接关系及必要的工作条件；
2. **实验方法**：测量思路、操作流程和关键操作；
3. **主要实验内容**：实际完成的测量、观察、记录和数据处理任务。

实验部分应使读者能够理解实验如何实施，不必机械重复讲义中的全部步骤，也不能把未实际完成的内容写成已完成。

### 四、结果与分析讨论

结果与分析讨论应以图、表和文字结合的方式呈现实验结果，并按以下四步组织。每一步都应根据实验数据和实际完成的内容撰写，不得用模板句代替分析。

1. **定量结果**：给出主要测量值、计算值、理论值或估计值及其单位，并在正文中解释相应图表。不能只写“结果如图所示”。曲线拟合时，应给出拟合关系、关键参数，以及 R²、残差或其他适当的拟合质量指标。
2. **规律与进一步处理**：分析数据呈现的规律，进行必要的定标、拟合、误差统计、插值或其他处理；给出代表性计算过程，使数据处理可以复核。
3. **与理论比较并解释**：结合实验原理，将实验结果与理论预期、标准值或理论趋势比较，说明观测现象和偏差的合理原因。
4. **误差来源与改进建议**：分析系统误差、随机误差、人为读数、环境因素、仪器精度、样品或装置状态等影响，并至少提出 **两条具体、可执行且与误差来源对应的改进建议**。

不确定度、误差估计和拟合参数应在适用时给出。若报告使用不确定度，必须说明误差传递式、标准差或其他计算依据。原始测量值、计算值、理论值和估计值应明确区分；不得为了迎合理想规律而删改异常数据。确需剔除数据时，应给出明确判据和理由，并保留原始记录。

### 五、结论和建议（总结）

总结全文，概括：

- 实验采用的主要方法；
- 得到的主要结果；
- 由结果支持的主要结论；
- 实验中存在的主要问题及改进建议。

结论必须直接回应实验目的，并以实验数据、实验现象或理论分析为依据。不能只写“实验成功”“结果合理”等没有证据支撑的判断，也不能超出实验数据支持范围。

### 六、参考文献

参考文献应列出报告中实际引用或参考的资料。无其他教师规定时，建议列出 **3—6 条**，优先使用实验讲义、课程教材和可靠的专业文献，并按正文首次引用顺序编号为 `[1]`、`[2]`……全文格式保持统一。

- 正文使用 LaTeX 的 `\cite{}` 生成顺序编码引用，引用号应紧跟相关论述，不以手工输入的编号替代；
- 文献表只保留正文实际引用的资料，并与正文第一次出现的先后顺序一致；
- 文献类型应明确标注，例如专著 `[M]`、期刊论文 `[J]`、报告 `[R]` 或实验讲义 `[实验讲义]`；
- 作者、题名、文献类型、出版地、出版社、年份、卷期和页码等适用字段应尽量完整，标点与中英文格式全文统一。

推荐格式如下，具体格式以教师要求为准：

1. **书籍**：`[序号] 作者．书名．出版地：出版社，出版年。`
2. **期刊文献**：`[序号] 作者．论文题目．期刊题目，年份，卷（期）：页码。`

## 原始数据与补充材料

- 原始测量数据、实验现象和计算过程应如实保留，不得只保留整理后的结果；
- 有清晰原始数据表或照片时，原则上在正文后附录；若教师要求教师签名位，应一并保留并说明位置；
- 程序、完整计算表、拟合图或数据图片可作为附录，正文只保留理解结果所必需的内容；
- 缺少姓名、日期、数据、图片、参数或其他材料时，使用“待补充”标记，并在报告末尾列出待补充清单。

- 程序、完整计算表、拟合图或数据图片可作为附录，正文只保留理解结果所必需的内容；- 附录表与数据文件一一对应：**同一份数据不得以不同表名重复列出**（例如既作"参数指标表"又作"计算结果汇总表"）；若确需区分，两者内容必须有实质不同，否则合并为一张表。
- 使用模拟/演示数据时，必须在**两处**声明来源：报告正文（封面信息栏下方、摘要之前的灰色提示框）与 `lab_report/data/README.md`；两者表述须一致，并写明"正式提交前须以实测原始记录替换"。

## 图、表和公式的规范

### 图的规范

- 报告中的所有图统一连续编号，例如“图 1”“图 2”；
- 所有图均采用双语图题：中文图题在前并作为主要说明，英文图题在后，以 `Fig. 1`、`Fig. 2` 等形式编号；
- 正文对图片的引用统一使用 `Fig.`，LaTeX 中写作 `Fig.~\ref{...}`，不混用“如图”“图 \ref{...}”等形式；
- 类型相似、需要并列比较的图应合并为子图，按 `(a)`、`(b)`……编号，并为每幅子图设置简洁的中文小标题；正文引用子图时使用 `Fig. 1a`、`Fig. 1b` 等形式；
- 图像内部的标题、坐标轴名称和图例尽可能使用中文；必要的物理符号、单位和通用英文缩写可以保留；
- 数据图的横轴和纵轴必须标明物理意义和单位；
- 曲线图应在图例或正文中说明数据点、拟合曲线和理论曲线各自的含义；
- 图应在正文中被引用和解释，不能只插入图而不说明结果；
- 图表应尽量放在首次引用它们的段落附近，保证分辨率、字号和细节清晰可读。

- 图题随所用模板：**默认（清华模板）** 使用单语中文图题“图N：描述”，正文以 `图~\ref{...}` 引用；**备选（物理学报模板）** 使用双语图题（中文主图题在前、英文 `Fig. N` 在后），正文统一 `Fig.~\ref{...}`；
- 同一份报告内的图题语言与引用形式必须统一，不得混用“如图”“图 \ref{...}”等写法；
- 类型相似、需要并列比较的图应合并为子图，按 `(a)`、`(b)`……编号，并为每幅子图设置简洁的中文小标题；正文引用子图时沿用同一模板的写法（如 `图~\ref{...}a` 或 `Fig.~\ref{...}a`）；
- **按最终尺寸出图**：绘图脚本的 `figsize` 宽度（英寸）应约等于最终插入宽度（cm）÷2.54；插入宽度不宜低于 **0.85 倍栏宽**（正文图推荐 `figure*` 通栏 + `width` ≥ 0.9`\textwidth`）。把 6 英寸宽的图缩到 3 英寸排入，会使 8–10pt 的字变成 4–5pt，读者无法辨认。
- 图内文字（坐标轴标签、刻度、图例）在**最终尺寸**下应不小于约 7–8 pt；坐标轴标签一律写“物理量 / 单位”（如“水平场磁感应强度 / G”）；多条曲线必须有图例；关键数值（峰值、拟合线、缺测点）应在图上标注。
- **图题必须自足**：说明画的是什么、横纵轴含义与单位、图例含义，以及读者能从中看出什么（关键结论或数值）——不看正文也能读懂。

### 表格的规范

- 报告中的所有表格统一编号，例如“表1”“表2”或“表1-1”“表1-2”；
- 所有表格必须有准确的表题，推荐统一写作“表X：描述”；
- 每一栏数据都应标明物理意义和单位，单位可放在栏标题中；
- 优先使用三线表，表格应避免竖线过多和无意义的装饰；
- 表格中的有效数字应与仪器精度和数据处理要求相符；
- 表格应在正文中被引用，并结合文字说明数据体现的现象或规律。

- **原始数据与派生量必须分表——“原始数据”表里只放原始读数**（如电流表读数、频率读数），均值、标准差、四方程解、拟合参数等派生量另表给出，并在表题/表注中写明由哪张原始表、按什么公式算得。曾有报告把两次课的不同物理量“熔化”进一张 10 列表：同一个 `重复次数` 列在第一次课是“同一条件 4 次重复测量”、在第二次课填的却是“4 个共振判读状态”，`标准差` 列同理，读者必然看不懂，而且**原始读数一个也没有出现在表里**（只在手写照片上）；
- **原始读数表必须带测量条件**：方向符号约定（“+”表示同向还是反向）、射频频率、重复次数、判读方式、缺测标注。缺测写在表内（“缺测”或表注），不要留整片空行；
- **矩阵形态优先**：同一条件下的多次重复测量应横向排成“第 1 次/第 2 次/…”列（4 组合 × 4 次 = 4 行 6 列），同一批状态的多个读数排成“状态 1/状态 2/…”列，而不是把每个读数摊成一行（避免 24 行长表与成片空行）；
- **表头一律写“物理量 / 单位”**（如“水平场电流 / A”），**不要用 ASCII 下划线**（如 `电流_A`）；
- **每张表列数 ≤ 7**；行数 > 20 时应拆分或移入附录，正文只放结论性表；
- **表内数值按列统一小数位**（如电流一律 3 位小数、换算系数一律 4 位）；**数值须四舍五入到与不确定度相称的位数**：`0.006071516782346937` 这类 19 位小数会把该列撑到 10em（≈3.5cm），整表宽度随之爆炸；
- **不要用 `\resizebox` / `\scalebox` 整体缩放表格**（会把字压到看不清）；改用 `table*` 通栏、`\small`/`\liuhao`、减少列数或拆表；
- **表宽必须落在版心内**：LaTeX 对“表格比版心宽”**不报错**，只把它排到纸外——实测一张 10 列表有 430pt（≈15cm）落在纸外、读者完全看不到，日志里仅有一条 Overfull hbox。宽表交给 `make_data_tables.py` 生成（已按版心自适应列宽并会给 `FAIL` 告警）；若仍放不下，**必须拆表**（拆成列数 ≤7 的多张表），不得依赖缩放；
- **浮动体（表/图）不得超过一页高度**：超出时 LaTeX 会把整张表挤出纸面（`Float too large for page`），表内容全部不可见——此时应拆表、缩短备注列或移入附录；
- 表题必须自足：写明数据来源与条件（哪次课、何种组合/状态、是否含重复测量）以及关键结论；

### 浮动体位置（表/图与正文的距离）

- **表、图必须紧跟引用它的正文**：`table*`/`figure*` 这类通栏浮动体只能排在页顶，每页最多一两个；通栏浮动体一多就排队，出现“正文说见表 5、表 5 却在 3 页之后”的糟糕体验（实测：13 页报告里 15 个通栏浮动体，表 5 与引用相隔 3 页、图 4 相隔 3 页）。
- 控制手段（按优先级）：① **减少通栏浮动体数量**——原始数据表集中放附录（附录 A 一次列全），正文只留结论性/派生结果表；同类结果表能合就合。② 每节末尾用 `\FloatBarrier`（`placeins`）拦住浮动体外漂；③ 导言区加载 `flafter`（浮动体不得出现在引用之前）与 `dblfloatfix`（改善通栏浮动体调度）；④ 浮动体位置参数用 `[!t]`。
- **验收口径**：编译后跑 `check_float_distance.py <pdf>`，正文中每个表/图与其**首次引用**的页距应 ≤1 页（附录内的表/图不受此限）。

### 公式的规范

- 公式采用独立居中的显示形式，按出现顺序连续编号，例如 `(1)`、`(2)`；
- 正文引用公式时使用“式（1）”等统一写法；
- 公式中出现的每个符号，在第一次出现时标明物理意义和单位；
- 说明公式的适用条件、近似或边界条件；
- 公式前后保留适度间距，公式编号不得与正文、图号或表号混淆；
- 代入实际数据时写清数值、单位和有效数字，保证量纲一致。

## 写作与排版实现

### 清华近代物理实验报告模板（thuemp）版面 —— 默认

- 实验报告默认使用工作区 `清华近物实验报告模板/` 中的 `thuemp.cls`（本 Skill `assets/thu_template/`；版本 v1.1，非官方模板），它以 `ctexart` 为基础、采用 **A4 双栏**排版；
- 首页结构固定：题目（`\emptitle`）→ 作者与指导教师（`\empauthor`）→ `【摘 要】`（`empAbstract` 环境）与 `【关键词】`（`\Keyword{}`）→ **英文题目/作者/摘要/关键词**（`\emptitleEn`、`\empauthorEn`、`empAbstractEn`、`\KeywordEn`）→ 首页脚注（`\empfirstfoot{实验时间}{报告时间}{学号}{E-mail}`）；
- 一级标题用中文编号「一、二、三」：导言区加 `\ctexset{section={name={,、},number=\chinese{section}}}`；
- 页眉由模板提供（“北京师范大学普通物理实验”）；正文页眉可写“<姓名>: <题目>实验报告”；
- 宽幅图、宽幅表与较宽公式使用 `table*`/`figure*` 跨栏；
- 参考文献按 **GB/T 7714—2015**：`\bibliographystyle{gbt7714-numerical}` + `\bibliography{./refs}`（BibTeX），正文用 `\cite{}`；
- 首页内容较多时，在正文第一节内加 `\enlargethispage{-3.3cm}`，避免正文与首页脚注框重叠；
- 编译链：`xelatex → bibtex → xelatex → xelatex`（含参考文献，必须跑 bibtex）。

### 物理学报模板式版面

- 实验报告默认参考工作区 `物理学报模板_Acta_Physica_Sinica_` 中的 LaTeX 模板组织标题、图题、正文引用和参考文献；只借鉴适合课程实验报告的版式，不照搬作者单位、基金、PACS 代码等期刊投稿信息；
- 第一页左上角使用工作区根目录的 `校徽.webp`。排版时可转换为 LaTeX 兼容的 PNG，但不得改变校徽比例、文字内容或造成拉伸；
- 校徽显示宽度设为 **7 cm**，相对于正文左页边距向左偏移 **2 cm**，只出现在第一页；
- 校徽采用叠加定位，不占用正文流，不应推动标题、基本信息栏或摘要的位置；编译后必须检查校徽没有超出页面、被裁切或遮挡标题。

- 仅在明确要求时使用：工作区 `物理学报模板_Acta_Physica_Sinica_/` + `report_preamble.tex`（`ctexart`，单栏）；
- 该模式下采用双语图题 `\bicaption{中文}{English}`、正文以 `Fig.~\ref{...}` 引用；
- 校徽：使用工作区根目录 `校徽.webp`（转 PNG 后由 `\bnulogo` 叠加插入），宽 **7 cm**、相对正文左页边距左移 **2 cm**、仅首页、不占正文流；编译后须检查未被裁切或遮挡标题。

### 写作风格

- 操作和结果优先使用完成时表述，例如“测量了”“得到”“结果表明”；
- 原理和普适规律使用现在时客观陈述；
- 操作过程可使用无主语句或自然的被动表达，但不强行套用英文式被动语态；
- 优先使用“测量”，根据语境区分“相对误差”和“相对偏差”；
- 物理量、符号、单位、量纲和有效数字全文保持一致；单位优先使用规范符号，避免在数字后混用中文单位名称；
- 使用清晰、准确、简洁的中文，必要时保留规范的物理术语、符号和英文缩写。

- **行文中不得出现括号**（用户硬要求：括号式旁注不符合人类行文逻辑）。改写办法：
  - 旁注/补充说明 → 并入句子，用逗号或“即”“也就是”连接，或另起一句；
  - 图表与公式引用 → 写“见表 3”“见图 1”“由第 2 式可得”，不写“（表 3）”；
  - 枚举序号 → 写“第一，…；第二，…”或 `enumerate` 环境，不写“（1）（2）”；
  - 小节标题里的“（一）（二）” → 写“一、二、”；
  - 单位提示 → 写“单位为 m”“以 m 为单位的有效半径”，或表头/表题写“…… / A”；
  - 不确定度 → 写“标准不确定度为 0.0012 G”，不写“(1.2345±0.0012) G”；
  - 附加说明（如“峰-谷系列为 5 个”）→ 用“；”分句或“其中……”从句。
  - **例外（可以保留）**：数学公式内部的分组括号（如 `(B_h^{+−}-B_h^{−+})/2`、`F(F+1)`）、
    `\eqref` 自动生成的编号括号、代码清单与文件名 `\texttt{}` 里的括号。
  - 检查：`check_report_tex.py` 会扫描行文括号并在告警里给出行号与片段（v1.27 起）；
    表题与表内文字同样受此约束——表题写在 `table_captions.json` / 数据准备脚本里。

### 页面与段落

- 使用 A4 纸张，页边距原则上设置为上、下、左、右各约 2 cm；
- 正文采用约 1.5 倍行距，页码位置统一，全文保持一致；
- 中文正文使用易读、统一的宋体或同等正文字体，一级标题使用黑体或同等醒目字体；
- 段落宜首行缩进两个汉字，避免通过连续空格强行对齐；
- 公式、图和表应尽量放置在首次引用它们的段落附近。

### 标题与摘要布局

- 实验题目居中、醒目；
- 标题下方第一行放置作者和学号，第二行放置指导老师和实验时间；两行采用整齐、统一的居中或对齐布局，不列班级；
- “摘要”“Abstract”和中英文“关键词”标题样式统一，摘要正文连续成段；
- 正文从摘要和关键词之后开始，章节之间保持清晰、适度的留白，不使用过多装饰性元素。

- “摘要”和“关键词”标题样式统一，摘要正文连续成段；

### 标题层级与栏式

- 一级标题沿用中文编号：“一、引言”“二、原理”“三、实验”等，字体、字号和间距统一；
- 二级标题可使用“1.1”“1.2”或“（1）”“（2）”等连续编号；
- 三级标题仅在确有必要时使用，不能为了拆分短段落而过度细分；
- 正文可参考模板采用双栏布局，但宽幅图、宽幅表、复杂公式或原始数据表可以跨栏或改用单栏；
- 不得因双栏排版造成图表过小、公式断裂、标题孤行或表格无法阅读；可读性优先于机械保持双栏。

教师提供的 Word 模板、字体字号、页边距、栏式、篇幅或提交方式等明确要求，应替代本节的建议。物理学报模板只作为版式参考，不要求照搬学术论文中的英文标题、作者单位、基金、PACS 代码或双语摘要。

教师提供的 Word 模板、字体字号、页边距、栏式、篇幅或提交方式等明确要求，应替代本节的建议。清华模板与物理学报模板都只作为版式参考：清华模板首面含英文标题/摘要/关键词（属其固定结构，内容须与中文一致）；两者均不照搬期刊投稿信息（作者单位、基金、PACS 代码等）。

### 排版实现片段（随 Skill 提供，已编译验证）

**默认：清华模板（thuemp）** —— 前置区骨架见 `assets/scaffold_template/report_frontmatter_thu.tex`（脚手架已复制为 `<实验标题>/lab_report/report_frontmatter_thu.tex`）。要点：

```latex
\documentclass{thuemp}
\usepackage{xcolor}                 % 数据说明灰框需要
\sisetup{detect-all,per-mode=symbol,separate-uncertainty=true}
\ctexset{section={name={,、},number=\chinese{section}}}   % 一、二、三
\Keyword{关键词1；关键词2；关键词3}
\begin{document}
\emptitle{实验题目}
\empauthor{姓名}{指导教师}
\fancyhead[CO]{{\footnotesize 姓名: 实验题目实验报告}}
\twocolumn[
\begin{@twocolumnfalse}
\maketitle
<数据说明灰框：\fcolorbox>
\begin{empAbstract} 摘要 \end{empAbstract}
\emptitleEn{...}\empauthorEn{...}{...}\KeywordEn{...}
\begin{empAbstractEn} English abstract \end{empAbstractEn}
\empfirstfoot{实验时间}{报告时间}{学号}{E-mail}
\end{@twocolumnfalse}
]
\wuhao
\section{引言} ...
```

- 图题：单语中文“图N：描述”，正文 `图~\ref{}`；表题“表N：描述”（可用 `\captionnamefont`/`\captiontitlefont` 统一字号）；
- 参考文献：`\bibliographystyle{gbt7714-numerical}` + `\bibliography{./refs}`（BibTeX）；
- 宽表/宽图用 `table*`/`figure*`；首页必要时 `\enlargethispage{-3.3cm}`；
- 数据表仍用 `tables/` 下由 `make_data_tables.py` 生成的片段，`\input{tables/<name>}` 引入，不要手抄。

**备选：物理学报模板**

备选模式下使用导言区片段 `report_preamble.tex`（脚手架同样复制到 `<实验标题>/lab_report/`）。主文件写法：

```latex
% !TEX program = xelatex
\documentclass[UTF8,a4paper,10pt]{ctexart}
\input{report_preamble}
\begin{document}
\thispagestyle{plain}
\bnulogo              % 首页左上角校徽（overlay，7.0cm，不占正文流）
...
```

该片段已包含并验证以下约定：

- 版式：A4、四边 2cm、1.5 倍行距、首行缩进 2 字符；
- 一级标题中文编号「一、二、三」（`\ctexset{section={name={,、},number=\chinese{section}}}`），与老师小论文格式一致；
- 双语图题：`\bicaption{中文主图题}{English caption}`；正文引用统一 `Fig.~\ref{}`；
- 不确定度与单位：`\SI{438.5(17)}{\mega\hertz}`、`\SI{0.35}{\percent}`、`\ang{89.8}`（`siunitx` 的 `separate-uncertainty`）；
- 封面信息栏：`\blankfield{4.0cm}` 留空横线，两行（作者/学号；指导老师/实验时间），**不列班级**；
- 校徽：`\bnulogo` 读取 `assets/bnu_logo.png`（脚手架已随实验复制），宽 7.0cm、相对正文左页边距左移 2cm、仅首页、overlay 不占流。

数据表请用 `tables/` 下由 `make_data_tables.py` 生成的片段，`\input{tables/<name>}` 引入，不要手抄。

## 提交前检查

- [ ] 报告抬头为完整实验题目；作者与学号位于第一行，指导老师与实验时间位于第二行，未列班级。
- [ ] 第一页左上角校徽宽 7 cm，相对正文左页边距左移 2 cm，比例正确且没有裁切、拉伸或遮挡正文。
- [ ] 中文摘要为 100—200 字，包含目的、方法、主要结果和主要结论；有真实数据时含至少一个关键数值。
- [ ] 中文摘要后有对应的英文摘要（Abstract），物理量、单位和关键数值与中文摘要一致。
- [ ] 中英文摘要后分别列出 3—5 个对应关键词。
- [ ] 正文包含引言、原理、实验、结果与分析讨论、结论和建议、参考文献六部分。
- [ ] 引言和原理完成必要的背景、模型、公式、符号、单位及适用条件说明。
- [ ] 原理部分经过理解和归纳，没有照抄讲义；实验部分只描述实际完成的内容。
- [ ] 原始数据、实验现象、单位、有效数字和计算过程完整且可复核。
- [ ] 缺失数据或信息已标记“待补充”，并列出待补充清单，没有编造内容。
- [ ] 结果讨论按定量结果、规律处理、理论比较、误差与改进四步展开。
- [ ] 使用拟合时给出拟合关系、参数及 R²、残差或其他适当的质量指标。
- [ ] 使用不确定度时给出传递式、标准差或其他计算依据。
- [ ] 至少提出两条具体改进建议，并能对应到误差来源或实验限制。
- [ ] 图、表、公式均已统一编号，并有相应题目、说明和正文引用；图具有中文主图题和英文 `Fig.` 图题，正文使用 `Fig.` 引用。
- [ ] 类型相似的图已在适合时合并为带 `(a)`、`(b)` 小标题的子图，图内标题、坐标轴和图例以中文为主。
- [ ] 图的坐标、表格栏位和公式符号均标明物理意义和单位。
- [ ] 异常数据没有被无理由删除；必要的剔除判据和理由已说明。
- [ ] 已比较实验结果与理论预期、标准值或理论趋势，并分析差异原因。
- [ ] 参考文献均在正文中以 `\cite{}` 实际引用，按首次引用顺序编号，文献类型、字段、格式和标点统一。
- [ ] 页面、行距、标题层级、段落缩进和栏式布局保持统一。
- [ ] 图表和公式在当前页面布局下清晰可读，没有断裂、过小或错位。
- [ ] LaTeX 已成功编译，编译日志、目录结构、公式、图表、中文排版和 PDF 版面均已检查。

- [ ] **物理学报备选模式**：第一页左上角校徽宽 7 cm、相对正文左页边距左移 2 cm，比例正确且没有裁切、拉伸或遮挡正文；**清华默认模式**：使用模板自带页眉，无需校徽，须核对页眉文字与题名一致。
- [ ] 摘要为 100—200 字，包含目的、方法、主要结果和主要结论；有真实数据时含至少一个关键数值。
- [ ] 摘要后有 3—5 个关键词。
- [ ] 图、表、公式均已统一编号，并有相应题目、说明和正文引用；**图题语言与引用形式与所选模板一致**（清华默认：中文“图N：描述”＋ `图~\ref{}`；物理学报备选：中文主图题＋英文 `Fig.` 图题）。
- [ ] **已直接量过成品 PDF，而不是只看编译退出码**：`check_pdf_geometry.py` 无 `FATAL`——没有元素越出纸面（会被裁掉）、没有越出版心出血、没有通栏长行重叠、没有横线穿过文字、图内文字在最终尺寸下不小于 7–8pt、位图 ≥150dpi。
- [ ] 编译日志里没有 `Float too large for page`（浮动体放不进一页＝整表被挤出纸面），`Overfull \hbox` 数量与最大宽度已知且可接受（一般来自不可断行的长串/公式）。

### 版式定稿：双栏 + 半栏宽“就地图表”（实测结论，2026-09-17 晚）

用户要求：**① 保持双栏；② 表中提到“图1”的那句话之后，图1 要立刻、紧贴着出现。**
两个要求都可以满足，做法如下（已实现并实测验收）：

**A. 浮动体声明紧跟“提及它的那句话”**（关键，不是改宏包而是改位置）

**A2. 一句话只能提一个图表**：若一句里同时出现“图3…图4…”或“…表5、表6、表7…”，把该句拆成**每句只提一个**图表，各自后跟自己的图/表；实测不拆时后一个会被前一个的高度顺排下移（图4 曾距其引用 402pt），拆句后降到 25–55pt。
把每个 `\begin{figure}[H]`／`\input{tables/…}` 放在**首次 `\ref{}` 所在句子的句末标点之后**，
而不是等整段结束再声明。实测效果：表1/表2/表3/表5 的题注距引用句 **25–55pt（约 2–3 行）**。
若声明放在整段之后，而该段跨栏，图表就会落到**邻栏**（实测表1 曾落到右栏）。

**B. 图表按“半栏最终尺寸”做，才能既紧贴又不臃肿**
- 图：`figsize` 宽度 = `\columnwidth`（双栏下约 3.16in），字号 8–8.5pt；
  插入用 `figure[H]` + `width=\columnwidth`（1:1，图内字不缩）。
  实测按 0.95\textwidth 出图再缩到半栏，图内文字只剩 6–8pt，正是“臃肿又宽松”的根源。
- 表：`table[H]` + `\scriptsize` + `\setlength{\tabcolsep}{3pt}` + `tabular*{\columnwidth}`；
  列数压到 4–6 列，表头缩写（如 87P/87V 表示 87Rb 峰-谷/谷-谷），
  单位、接法约定、缺测说明等移入**表题**。`make_data_tables.py --column` 自动生成。
- 半栏表的可用宽度需按 `\scriptsize` 折算（21em@10.5pt ≈ 29.5em@7.5pt），
  否则会把放得下的表误判为“放不下”。

**C. 为什么半栏 `[H]` 不会像通栏就地表那样留洞**
`[H]` 插在某一栏内，只影响**该栏**的文字流；另一栏继续正常排版，所以不会出现整页空白。
通栏（`table*`/`strip`）才会因为整页宽度被占用而留洞。

**D. 验收口径（脚本化）**
- `check_inline_distance.py <pdf>`：量“引用行 → 该图/表题注”的**垂直距离**（同栏），
  目标 ≤60–70pt；跨栏会单独列出。
- `check_float_distance.py <pdf>`：页级距离，目标 ≤1 页（半栏就地图通常 0 页）。
- `check_pdf_geometry.py <pdf>`：越纸/出血/重叠/穿字/图内小字（<8pt）/低分辨率。

**E. 失败尝试（勿重蹈，均已回退）**：`\FloatBarrier`+`flafter`+`dblfloatfix`（16 页、4 处大空白）；
浮动体“交错”到段后（末页孤行）；双栏通栏 `strip` 就地表（放不下即整页留洞）；
数据图就地（两处 208pt 空白）；单栏 `table[H]`（4 处留洞）；整篇改单栏（用户不接受，且非课程唯一惯例）。

### 表格与图内标注的两个坑（2026-09-18 实测）

- **表格单元格/表头可以写 `$...$` 公式**（如 `$B_{res}$/G`、`$g_F$`、`$k$`）：
  `make_data_tables.py` 的 `tex_escape()` 现在**保留 `$...$` 片段不做转义**，只转义数学之外的
  特殊字符。此前一并转义会把表头渲染成字面的 `$B_{res}$/G`（实测 10 行如此），并且因为
  `\_`/`\{` 让整串不可断行，在窄 p 列里还会被折成两行、看起来像“文本重叠”。
- **半栏图的图内标注只能放极短文本**：`\columnwidth` 图上若有 4 个类目，每个类目横向仅约
  **42pt**；`均值 0.12345 A`（约 96pt）或 `0.1234±0.0025`（约 53pt）都会与相邻类目标注**相撞**
  （实测重叠 10.9pt）。正确做法：图内只标 `0.1234` 这类短数字，标准差交给误差棒并在图题说明。
- 排查方法：对成品 PDF 逐页做“任意两行 bbox 相交”扫描（`dx>2 且 dy>1`）。注意
  **数学分式的分子/分母 bbox 天然相交但字形不重叠**，属误报；工程判据以
  `check_pdf_geometry.py` 的校准阈值为准。
