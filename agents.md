# AGENTS.md

## 工作区用途

本工作区用于撰写物理实验预习报告和实验报告。

## 文件分工

- `preview.md`：物理实验预习报告的写作要求与检查清单。
- `report.md`：物理实验报告的写作要求与检查清单。
- 本文件：适用于整个工作区的总要求。

## 持续整理规则

- 用户后续发送的工作区总要求、长期写作偏好和协作规则，应自动整理后补充到本文件；如内容属于预习报告或实验报告的专门要求，则同步整理到相应的 `preview.md` 或 `report.md`。
- 收到实验讲义后，先识别其中的实验划分、实验名称和必要的上下文，再按实验建立独立的子文件夹。
- 每个实验的相关材料和报告应放在该实验自己的子文件夹中，避免不同实验的内容、数据和结论混在一起。
- 每个实验子文件夹至少应维护对应的预习报告和实验报告；文件命名和目录结构应清晰、稳定，并与实验名称一致。
- 预习报告和实验报告的正式文档均须使用 LaTeX 编写；`preview.md` 和 `report.md` 只作为要求与检查清单，不作为最终报告文件。
- 每次完成或修改 LaTeX 报告后，应自动编译生成 PDF，并检查编译日志、目录结构、公式、图表和中文排版。
- LaTeX 源文件、编译生成的 PDF 及相关实验材料应保存在对应实验的独立子文件夹中；编译辅助文件应集中管理，避免污染工作区。
- 编译优先使用支持中文的 LaTeX 引擎（如 XeLaTeX 或 LuaLaTeX），并根据模板需要运行 BibTeX/Biber 等参考文献工具。
- 报告只有在成功编译并完成基本版面检查后，才视为完成；若因缺少材料、数据、宏包或其他环境问题无法编译，应保留源文件并明确说明阻塞原因。
- 如果讲义中的实验边界、名称或目录结构存在歧义，先依据上下文作出最小假设，并在整理结果中标明需要确认的部分。
- 不因建立目录而虚构实验内容、数据或结果；讲义中缺失的内容保留为待补充项。

## 总要求

- 以用户提供的实验指导书、课程模板和教师要求为最高优先级；若它们与本文件冲突，以具体要求为准。
- 不编造实验数据、测量结果、误差、图表或实验现象。缺少信息时应明确标注待补充，并向用户索取必要内容。
- 区分原始测量值、计算值、理论值和估计值；计算过程应可复核。
- 物理量必须标明单位，公式、符号、有效数字和量纲应保持一致。
- 结论应由实验数据或明确的理论推导支持，避免超出证据范围的表述。
- 保留原始记录，修改数据或格式时不得掩盖异常值；异常情况应在分析中说明理由。
- 使用清晰、准确、简洁的中文表述，必要时保留规范的物理术语、符号和英文缩写。
- 生成或修改文档后，检查公式、单位、数值、图表、引用和章节结构，并报告尚未解决的缺失信息。

## 协作约定

- 开始工作前，先阅读与当前任务直接相关的要求文件和实验材料。
- 只修改完成任务所必需的文件，保留无关的现有改动。
- 优先使用小范围、可审查的修改；避免未经授权的删除、覆盖或重置操作。
- 如果遇到会显著改变结果的缺失信息、破坏性操作或外部副作用，应先向用户确认。
